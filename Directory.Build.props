<Project>
  <PropertyGroup>
    <!-- Keep NuGet resolution reproducible. Each project commits its generated packages.lock.json. -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
  </PropertyGroup>
</Project>
